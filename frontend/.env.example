# In development, requests to /api/* are proxied to http://localhost:8000
# by vite.config.js -- no configuration needed.
#
# For a production build, point this at the deployed backend's real origin
# and copy this file to .env.production before running `npm run build`.
VITE_API_BASE_URL=https://your-backend-domain.example.com
