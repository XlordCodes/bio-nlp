# requirements.txt
# ------------------
# Exact versions verified working together in this project's development/
# test environment (Python 3.12). Install with:
#   pip install -r requirements.txt
#
# Badread is not published on PyPI under a plain `badread` name -- it's
# installed directly from its GitHub source, pinned to the exact tagged
# release (v0.4.2) that was tested, not an unpinned branch HEAD.
#
# minimap2 is a SYSTEM BINARY, not a Python package, and cannot go in this
# file. Install it via your OS package manager, e.g.:
#   apt-get install minimap2
# (mappy, below, is minimap2's separate Python-bindings package and is
# independent of the CLI binary -- both are used by this project, for
# different purposes: mappy for programmatic alignment in
# data/simulator.py and training/metrics.py's internal checks, the CLI
# binary for training/metrics.py's external verification subprocess call.)

# --- Deep learning ---------------------------------------------------------
torch==2.12.1

# --- Backend API -------------------------------------------------------------
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
python-multipart==0.0.32

# --- Bioinformatics ------------------------------------------------------------
edlib==1.3.9.post1
mappy==2.31
badread @ git+https://github.com/rrwick/Badread.git@v0.4.2

# --- Test-only ---------------------------------------------------------------
# Required by fastapi.testclient.TestClient, used in backend/main.py's own
# __main__ sanity-check block (`python -m backend.main`). Not needed to run
# the API itself, only to run its self-tests.
#
# NOTE: in this project's development sandbox, starlette's TestClient
# reported needing a package called "httpx2" rather than the standard,
# widely-documented "httpx" -- an unfamiliar, unverified requirement this
# assistant cannot confirm is a normal/expected part of the public FastAPI
# ecosystem as of this writing. `httpx` is pinned below as the standard,
# well-established package. If `python -m backend.main` fails with an
# error asking for a different package name in your environment, follow
# that error message directly rather than assuming this file is wrong --
# and treat it as a sign your starlette/fastapi versions may be newer than
# what's documented here.
httpx==0.28.1
pytest==9.1.1  # runs tests/ -- see that directory's own notes on what is/isn't covered there
